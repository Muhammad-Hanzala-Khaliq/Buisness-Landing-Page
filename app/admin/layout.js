export const metadata = {
  title: "Admin Dashboard - Leads Management",
  description: "Secure admin panel to manage clinic leads and inquiries",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
